{% extends 'base.html.twig' %}

{% block body %}
    {{ cms_area('header') }}
    {{ cms_area('header', _opera_page) }}
    <div class="container">
        {{ cms_area('body', _opera_page) }}
    </div>
{% endblock %}
